function isValidSubsequence(array, sequence) {

    let temp = [];
    temp.push(...sequence);

    console.log(temp);
}

const map = [1, 2, 3, 4, 5, 6]
const map2 = [2, 3, 4, 5];

isValidSubsequence(map, map2);
